# Agent instructions

## Upstream synchronization

When asked to synchronize with `huggingface/swift-transformers`, use the project-local `$sync-upstream` skill. Limit the review to tokenizer code, maintain the upstream ledger, and treat commits, pushes, and pull requests as separately authorized actions.
