interface:
  display_name: "Sync Upstream"
  short_description: "Evaluate and adapt tokenizer upstream changes"
  default_prompt: "Use $sync-upstream to evaluate the next swift-tokenizers upstream changes and apply only the approved adaptations."
